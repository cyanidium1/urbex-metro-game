import React from "react";
import { Text } from "react-native";

const ScnDescription = ({ text }) => {
  return (
    <Text className="text-base text-white font-bold text-center">{text}</Text>
  );
};

export default ScnDescription;
